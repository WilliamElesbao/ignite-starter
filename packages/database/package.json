{
  "name": "@repo/database",
  "scripts": {
    "build": "",
    "migrate:dev": "bunx --bun prisma migrate dev",
    "model:generate": "bunx --bun prisma generate",
    "prisma:studio": "bunx --bun prisma studio",
    "db:seed": ""
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
